#!/usr/bin/env sh
# Navigeer naar db-flow/ want git draait hooks vanuit de repo-root
cd "$(git rev-parse --show-toplevel)/db-flow" || exit 1

echo "🔍 Pre-commit: ESLint + TypeScript check..."
npx lint-staged
